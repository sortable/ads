import { assert } from 'chai';
import Service from './service';

describe('test Service class', () => {

  it('should emit "noUnitDefined" event when defineUnit service return null or undefined', async () => {
    // TODO
  });

  it('it should emit error event when config.requestHB has an exception', async () => {
    // TODO
  });

  it('it should emit error event when config.requestGPT has an exception', async () => {
    // TODO
  });

});
